<tr id="item-<%= item.id %>">
    <td><%= item.name %></td>
    <td><%= distance_of_time_in_words(item.created_at, Time.now - 7.days) %></td>
    <td><%= link_to " ",[item.user, item], method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %></td>
</tr>